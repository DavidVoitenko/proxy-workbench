# HANDOFF: владение файлами и передача изменений

**База:** ветка `integration/ultra-2026-09-25`, HEAD `9406372`, 25 сентября 2026 года.
**Сопутствующий документ:** `docs/integration/CONTRACTS.ru.md` — источник истины по идентичностям, схеме, freshness, snapshot, API и состояниям job. Здесь — только владение файлами и порядок передачи.

## 0. Правило

**У изменяемого файла ровно один writer.** Исполнитель, которому файл не принадлежит, его не редактирует — ни частично, ни «на пять минут», ни «там одна строчка». Он приносит изменение через `docs/integration/HANDOFF/<имя>.md`, и файл правит владелец.

Это прямо следует из MASTER-PROMPT §1: «`proxytool.py`, `gui.py`, `api.py`, общая БД и центральные UI-файлы нельзя редактировать конкурирующим набором исполнителей. Минимальное выделение реальных модулей допускается для устранения конфликтов, без массовой переписи».

**Минимальное выделение новых модулей — да. Массовая перепись существующих файлов — нет.** Новый leaf-модуль (`db.py`, `jobs.py`, `secrets.py` и подобные) — это нормальный способ распараллелить работу. Копирование чужого модуля к себе, второй движок сканирования, вторая БД, второй worker-протокол — это не выделение, это дублирование, и на интеграции оно удаляется.

## 1. Единый интегратор: общие файлы

**У следующих файлов один владелец — интегратор. Больше никто их не редактирует:**

| Файл | Почему он общий | Что интегратор в нём делает |
| --- | --- | --- |
| `proxy_workbench/proxytool.py` (2543 строки) | Здесь `open_db` (`proxytool.py:552`) — единственная точка DDL; `scan` (`proxytool.py:1283`); `store` (`proxytool.py:1380`); `export` (`proxytool.py:1651`); публикация поколения (`proxytool.py:1898-1925`); `read_selection_file` (`proxytool.py:2249`); весь CLI (`main`, `proxytool.py:2273`); watch-цикл (`proxytool.py:2500-2511`) | Подключает новые модули к существующему движку, применяет точечные патчи из handoff, закрывает дефекты, живущие в этих строках |
| `proxy_workbench/api.py` (353 строки) | Здесь `Exports` (`api.py:93-195`), который импортируют `gui.py:234` и `proxytool.py:2195`; `public_row` (`api.py:51`); `parse_query`/`select` (`api.py:199-246`); read-only сервер (`api.py:249-353`) | Версионированный `/v1`, контракт ошибок, курсоры, права, маршруты ключей и заданий |
| `proxy_workbench/gui.py` (1164 строки) | Единый routes-файл: `do_GET` (`gui.py:963-1027`) и `do_POST` (`gui.py:1029-1063`); здесь же `App.start` (`gui.py:369`), `data_lock` (`gui.py:363`), скачивание (`gui.py:993-1024`), `gateway_state` (`gui.py:299`), запуск шлюза и API (`gui.py:1089-1147`) | Монтирует новые маршруты, снимает writer-lock с read-пути, разделяет secrets |
| `proxy_workbench/ui/app.js` (230 КБ) | Пользовательский визуальный и поведенческий язык; читается всеми экранами и тестами i18n | Только аддитивные правки, полученные через handoff, в существующем языке |
| `proxy_workbench/ui/index.html` (88 КБ) | Базовая разметка пользователя | Точечное добавление контролов, без редизайна |
| `proxy_workbench/ui/style.css` (140 КБ) | Базовая визуальная система | Точечное добавление правил для новых контролов, без редизайна |

### 1.1. Что это значит на практике

- Исполнитель, которому нужен новый маршрут GUI, **не пишет** `gui.py`. Он пишет в своём модуле обработчик с явной сигнатурой и в handoff указывает: `gui.py`, `do_POST`, добавить ветку `'/api/<x>'` → `self.app.<y>(payload)`. Интегратор добавляет одну-две строки.
- Исполнитель, которому нужен новый контрол в UI, **не пишет** `index.html`/`app.js`. Он отдаёт разметку и обработчик в своём handoff, с указанием, в какой существующий контейнер вставляется и какими существующими классами оформляется. Интегратор вставляет.
- Дизайн пользователя — база. Добавление нового в том же языке — да; общий редизайн, смена тем, переписывание layout — нет (MASTER-PROMPT §0, §4 F26).

### 1.2. Узкие точки, за которые отвечает интегратор

Это перечень мест, к которым сводятся все запросы. Исполнители не правят их, а формулируют требование в handoff.

Позиции в `proxy_workbench/ui/*` указаны поимённо — функцией или `id` элемента, — потому что на момент написания эти три файла изменены параллельным исполнителем и номера строк в них смещаются. Перед правкой интегратор ищет якорь по имени, а не по номеру строки.

| Место | Что туда подключается |
| --- | --- |
| `proxytool.py:552-571` (`open_db`) | Единственное место, которому вообще разрешено писать DDL. Версия схемы, порядок миграций, `application_id`/`user_version` |
| `proxytool.py:648-667` (`collect.add`), `:678-695` | Единая модель endpoint и коллекции при сборе |
| `proxytool.py:1283-1506` (`scan`) | Обязательный `collection_id` в scope; `access_id` в ключе строки; запись `valid_until` в `store` |
| `proxytool.py:1335-1340` (`done`) | Повторная проверка истёкших: дефект 2 |
| `proxytool.py:1380-1391` (`store`) | Политика freshness при измерении, список колонок вместо позиционной вставки |
| `proxytool.py:1651-1932` (`export`) | `kind='published'/'selection'/'diagnostic'`; разделение `state` и `stop_reason`; `collection_id` в `status.json` |
| `proxytool.py:2323-2335` | `workbench.lock`: граница writer-lock |
| `proxytool.py:2464-2511` (`run_scan` + watch) | Передача `want` в watch-цикл; refill; дефект 12 |
| `api.py:51-89` (`public_row`) | Форма строки: `age_seconds`, `admission_reason`, per-zone коды DNSBL, байты/длительность замера |
| `api.py:93-195` (`Exports`) | Проверка `schema_version` поколения, чтение manifest, отказ вместо молчаливой выдачи |
| `api.py:181-182` | Снятие статусного обнуления построчного списка; дефект 3 |
| `api.py:249-353` (`make_api_server`) | `/v1`, 202, SSE, idempotency, ETag/revision, rate limit, лимиты тела |
| `gui.py:963-1063` (`do_GET`/`do_POST`) | Монтирование новых маршрутов; снятие `data_lock` с `/api/results`, `/api/result-detail`, `/api/download/*` |
| `gui.py:1129-1134`, `:1089` | Раздельные `gateway_token` и `gui_session`; LAN opt-in вместо `0.0.0.0` по умолчанию |
| `ui/app.js`, контейнер `#live-ticker-list` (живая лента) | Перевод на реальные события вместо regex по логу; дефект 25 |
| `ui/app.js`, функция `downloadFile` | Одно закрытие stream, picker в пользовательской активации, cancel без ошибки; дефект 21 |
| `ui/app.js`, обработчик карточек сценариев + `ui/index.html`, блок карточек сценариев | Полный сброс полей при смене сценария; дефект 24 |
| `ui/index.html`, `#selection-action-bar` + `ui/app.js`, обработчики `#action-*` (bulk-панель) | `page`/`selected`/`all-matching` scope, сброс выделения при смене scope |

## 2. Модульные исполнители: новые файлы

Каждый из этих файлов — **leaf-модуль, единственный writer — его автор**. Он не импортирует и не мутирует чужие модули без явной точки входа; он экспортирует чистые функции и узкие типы, на которые ссылаются остальные. Каждый ведёт `HANDOFF/<имя>-handoff.ru.md` и свои тесты `tests/test_<имя>_*.py`, запускаемые командой `.venv/bin/python -m unittest tests.test_<имя>`.

| Модуль | Исполнитель | Модуль | Исполнитель |
| --- | --- | --- | --- |
| `proxy_workbench/db.py` | Хранилище и миграции | `proxy_workbench/pools.py` | Постоянные пулы и квоты |
| `proxy_workbench/core.py` | Admission и freshness | `proxy_workbench/scheduler.py` | Расписания, quiet hours, бюджеты |
| `proxy_workbench/probes.py` | Режимы проверки и измерения | `proxy_workbench/secrets.py` | Secret store, доступ, upstream auth |
| `proxy_workbench/jobs.py` | Job/item lifecycle | `proxy_workbench/apikeys.py` | Ключи, права, audit |
| `proxy_workbench/importer.py` | Импорт, preview, merge/replace | `proxy_workbench/apiv1.py` | `/v1`, операции, OpenAPI |
| `proxy_workbench/profiles.py` | Именованные профили и правила целей | `proxy_workbench/sourcedesk.py` | Приёмка чужого sources-workflow, F27 |
| `proxy_workbench/servicecatalog.py` | Каталог сервисов и наборов | `proxy_workbench/geo.py` | География и характеристики |
| `proxy_workbench/exportsvc.py` | Snapshot-контракт и артефакты | `proxy_workbench/diagnostics.py` | Воронка, коды ошибок, help |
| `proxy_workbench/pipeline.py` | Конвейер, параллелизм, бюджеты | `proxy_workbench/desktop.py` | Поставка и жизненный цикл |

Правила совместимости между модулями:

- Модуль **не создаёт** вторую БД, второй сетевой загрузчик, второй нормализатор прокси и второй worker-протокол. Точка нормализации и сетевого входа — `proxytool.py` (интегратор подключает).
- Схему своей таблицы модуль объявляет в своём handoff, а создаёт её через миграционный раннер `db.py`; право писать DDL в `open_db` есть только у интегратора.
- Если два модуля нуждаются в одном и том же, это сигнал, что не хватает третьего leaf-модуля, а не повод обойтись копией.
- Модуль, которому нужна правка чужого файла, описывает её в handoff и **не ждёт**: продолжает делать всё независимое, включая тесты на mocks и фикстурах.

## 3. Поверхности, которым принадлежит существующий файл целиком

| Владелец | Файлы | Граница |
| --- | --- | --- |
| Владелец шлюза | `proxy_workbench/gateway.py` (600 строк) | `Pool` (`gateway.py:85-184`), `Gateway.connect` (`:336`), `handle`/`handle_http`/`handle_socks5` (`:398-528`), `start` (`:531`), `Background` (`:543-600`). Слот до `await`, общий deadline handshake, health ≠ TCP-open, sticky-режимы, health-aware выбор, изоляция сессий, политика shutdown |
| Владелец репутации/измерений | `proxy_workbench/reputation.py` (307 строк), `proxy_workbench/anonymity.py` (156 строк) | IPv6 reverse для DNSBL, zone-specific коды, различение quota/access error, валидация judge до `elite`, `unknown` вместо выдуманного `clean` |
| Владелец данных и упаковки | `proxy_workbench/paths.py`, `proxy_workbench/maintenance.py`, `packaging/`, `run.sh`, `Start.bat`, `Start.command`, `docs/packaging/`, `.github/workflows/` | Выбор и перенос data path, portable mode, `RUNTIME_FILES`, сборка и CI |

Оговорка по `maintenance.py`: `RUNTIME_FILES`/`RUNTIME_DIRS` (`maintenance.py:14-32`) дописываются **только** владельцем этого файла. Модуль, которому нужен свой runtime-файл, пишет об этом в handoff одной строкой и не правит список сам.

## 4. Формат handoff

Каждый файл `docs/integration/HANDOFF/<имя>.md` — это запрос на изменение чужого файла, а не отчёт о работе. Обязательные поля:

```
# Handoff: <имя>

**Требования:** F.., дефект ..
**Контракт:** CONTRACTS.ru.md §.. (версия 1)
**База:** ревизия, на которой писался модуль

## 1. Прошу внести в <чужой файл>
- <файл>:<строка или функция>
- что именно заменить/добавить (конкретный вызов, сигнатура, тип)
- почему именно здесь (контрактная ссылка)

## 2. Что уже сделано у меня
- публичный API моего модуля, на который это опирается
- имя функции, которую надо вызвать, её аргументы и что она возвращает

## 3. Совместимость
- что ломается, если не внести (конкретный сценарий)
- что НЕ ломается: обратная совместимость, legacy-данные, существующие тесты

## 4. Проверки
- точная команда и её результат
- что осталось непрочитанным

## 5. Открытые вопросы
```

Handoff без пунктов 1 и 4 — не handoff. Handoff, который переписывает чужой модуль целиком, отклоняется: он нарушает правило одного writer и означает, что границу модулей надо проверить заново.

## 5. Запрещённые зоны

- `/Users/main/Desktop/111/proxy-workbench-sources` — отдельная рабочая копия чужого workflow. **Ни одного файла там не редактируется**, только чтение. `sourcedesk.py` готовит контракт приёмки, а не переписывает чужую работу.
- `proxy_workbench/ui/**` как дизайн: нельзя запускать общий редизайн, менять темы, layout, визуальный язык или переписывать существующие сценарии ради эстетики (MASTER-PROMPT §0).
- Полный `unittest discover -s tests` не запускается исполнителями: каждый гоняет только свой участок командой `.venv/bin/python -m unittest tests.test_<модуль>`. Полный набор один раз выполняет интегратор на собранной ревизии.
- `git reset --hard`, `git clean`, `git stash`, `checkout` по всему дереву не выполняются. Только адресные операции со своими файлами.
- Секреты: никаких настоящих паролей, токенов и учётных данных. Canary-секреты в тестах обязаны отсутствовать в БД, логах, `argv`, обычных файлах и JSON.
- Никаких массовых проверок публичных прокси, DNSBL и сторонних сервисов. Доказательства — локальные контролируемые сценарии, моки и фикстуры.

## 6. Разрешение конфликтов

1. Конфликт по содержанию — не по строкам. Два исполнителя хотят разного поведения: решение принимает владелец контракта (интегратор) на основании `CONTRACTS.ru.md`, и результат пишется туда же.
2. Конфликт по владению файлом: handoff отправляется владельцу файла. Если владелец не отвечает, изменение не применяется — файл остаётся в последнем согласованном состоянии. Молчаливое «кто быстрее нажал commit» не является разрешением.
3. Конфликт схемы: версия схемы и порядок миграций определены в `CONTRACTS.ru.md` §3.3. Исполнитель, которому нужна новая таблица, заявляет её в handoff; он не правит `open_db` сам. Две параллельные версии схемы — самая дорогая ошибка этого этапа, потому что она обнаруживается только на полном прогоне тестов.
4. Изменение общего контракта обсуждается с непосредственными потребителями и отражается в их проверках **до** интеграции. Не требуется общий совет всех исполнителей по каждому локальному изменению — достаточно, чтобы потребитель нового имени поля или нового кода ошибки обновил свои тесты.
5. Индекс git может быть занят: при `index.lock` — подождать и повторить один раз; если не вышло, оставить файлы незакоммиченными, интегратор соберёт их сам.

## 7. Как интегратор это применяет

Порядок интеграции, который следует из владения выше:

1. Прочитать `CONTRACTS.ru.md`, этот файл и все файлы в `docs/integration/HANDOFF/`.
2. Создать схему и прогнать миграции (миграции 0–12 из `CONTRACTS.ru.md` §3.3) — раньше, чем подключать модули, которым нужны их таблицы.
3. Подключить модули к существующему движку: сканирование, хранение, отбор по свежести, экспорт, задания, пулы, профили, коллекции, импорт, источники, ключи, API должны **использовать** их, а не дублировать логику рядом. Обходные пути, обходящие контракт, удаляются или делаются тонкими.
4. Применить точечные патчи из handoff по таблице §1.2. Патч, который переписывает чужой модуль, отклоняется и возвращается автору.
5. Закрыть дефекты, живущие в его собственных строках: 1, 2, 4, 6, 7, 8, 9, 20.
6. Собрать `/v1` с compatibility path для legacy read-only токена — без расширения его прав.
7. Согласовать error codes, единицы, permissions и policy между CLI, GUI и API.
8. Создать `tests/test_acceptance.py` по сквозным сценариям из MASTER-PROMPT §7.
9. Запустить `.venv/bin/python -m unittest discover -s tests` и `.venv/bin/python packaging/smoke.py .venv/bin/proxy-workbench` на интегрированной ревизии.

## 8. Известное расхождение с исполняемым workflow, которое нужно закрыть до интеграции

В `.zcode/workflow-drafts/ultra-2026-09-25.dwf.ts:737-738` поверхности «Интерфейс и список результатов» выдано прямое владение `proxy_workbench/gui.py`, `ui/app.js`, `ui/index.html`, `ui/style.css` и `i18n.py`. Это **прямо противоречит** и этому документу, и MASTER-PROMPT §1 («у изменяемого общего файла один writer»), и назначению интегратора в `.zcode/workflow-drafts/ultra-2026-09-25.dwf.ts:823-824`, который закрепляет за ним только `proxytool.py` и `api.py`.

Если оставить оба назначения, `gui.py` и три UI-файла будут записаны двумя агентами одновременно, и конфликт разрешится по времени, а не по поведению. Разрешение одно из двух, и выбирать его нужно до начала параллельной реализации:

- **вариант A (предпочтительный, соответствует этому документу):** правится workflow-draft, и «Интерфейс и список результатов» переводится в режим «свои новые файлы + handoff». Визуальная работа при этом не теряется: исполнитель отдаёт разметку, стили и обработчики в своём handoff с указанием существующих контейнеров и классов, а интегратор вставляет их в `index.html`/`app.js`/`style.css` без редизайна.
- **вариант B:** владение `gui.py` и `ui/*` остаётся за поверхностью, а за интегратором закрепляется `proxytool.py`, `api.py` и монтирование. Тогда владение `gui.py`/`ui/*` должно быть единственным и явно записанным, а в этом документе — исправлено, чтобы не расходились.

Пока вариант не выбран, действует консервативное правило: **правки в `gui.py` и `ui/*` не коммитятся ни одной из сторон, а копятся в handoff** — так цена ошибки минимальна, независимо от того, кто в итоге станет writer'ом.
